<types namespace="_idio">
  <import from="@goa/compress" ns="_goa" name="CompressConfig" />

  <record name="CompressOptions" noToc>
    <prop boolean name="use" default="false">
      Use this middleware for every request.
    </prop>
    <prop opt type="!_goa.CompressConfig" name="config">
      The `compress` configuration.
    </prop>
  </record>
</types>