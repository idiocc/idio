<types namespace="_idio">
  <record name="StaticOptions" extends="_idio.StaticConfig" desc="Top-level options when setting up static middleware.">
    <prop type="string|!Array<string>" name="root">
      Root or multiple roots from which to serve files.
    </prop>
    <prop boolean name="use" default="false">
      Use this middleware for every request.
    </prop>
    <prop string name="mount" default="/">
      Path from which to serve files.
    </prop>
  </record>
</types>