<types namespace="_idio">
  <type record name="StaticOptions" desc="The top-level options when setting up the static middleware.">
    <prop type="(string|!Array<string>)" name="root">
      Root or multiple roots from which to serve files.
    </prop>
    <prop boolean name="use" default="false">
      Use this middleware for every request.
    </prop>
    <prop string name="mount" default="/">
      Path from which to serve files.
    </prop>
    <prop number name="maxage" default="0">
      How long to cache files for.
    </prop>
    <prop opt type="_idio.KoaStaticConfig" name="config">
      `koa-static` configuration.
    </prop>
  </type>
</types>