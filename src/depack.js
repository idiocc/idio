import startApp, { createApp, compose, httpErrors, mount } from './'

DEPACK_EXPORT = {
  'startApp': startApp,
  'createApp': createApp,
  'compose': compose,
  'httpErrors': httpErrors,
  'mount': mount,
}